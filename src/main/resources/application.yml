spring:
  redis:
    host: redis
    port: 6379
  r2dbc:
    url: r2dbc:mariadb://mariadb:3306/kooapi?useUnicode=true&characterEncoding=utf8
    username: root
    password: 6599
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    show-sql: true

discord:
  oAuth2:
    client_id: ${discord.oAuth2.client_id}
    client_secret: ${discord.oAuth2.client_secret}
    redirect_uri: 'http://127.0.0.1:8080/login/oauth2/code/discord'

logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG
